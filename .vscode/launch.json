{
    "version": "0.2.0",
    "configurations": [
        {
            "outFiles": [
                "${workspaceFolder}/out/**/*.js"
            ],
            "name": "Run Extension",
            "request": "launch",
            "preLaunchTask": "npm: compile",
            "type": "extensionHost",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ]
        }
    ]
}
